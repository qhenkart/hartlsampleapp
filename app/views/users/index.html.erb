<% provide(:title, "All users") %>
<h1> All users </h1>

<div class='center'>
	<%= will_paginate @users, renderer: BootstrapPagination::Rails %>
</div>
<ul class="users">
	<%= render @users %>
</ul>


<div class='center'>
	<%= will_paginate @users, renderer: BootstrapPagination::Rails %>
</div>